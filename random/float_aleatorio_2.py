import random

# O random.random(a,b) gera um número flutuante,decimal aleatório entre 0.0 e 1.0

numeros = random.random()
print(f'numero: {numeros}')

numero_arred = random.random()
print(f'numero arredondado : {numero_arred:.2f}')